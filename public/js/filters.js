// Generated by CoffeeScript 1.6.2
'use strict';angular.module('gottaGo.filters', []).filter('interpolate', function(version) {
  return function(text) {
    return String(text).replace(/\%VERSION\%/mg, version);
  };
}).filter('double', function() {
  return function(input) {
    if (input.toString().length < 2) {
      return "0" + input;
    } else {
      return input;
    }
  };
}).filter('ms', function() {
  return function(s) {
    var addZ, hrs, mins, ms, secs;

    addZ = function(n) {
      if (n < 10) {
        return '0' + n;
      } else {
        return '' + n;
      }
    };
    ms = s % 1000;
    s = (s - ms) / 1000;
    secs = s % 60;
    s = (s - secs) / 60;
    mins = s % 60;
    hrs = (s - mins) / 60;
    return addZ(hrs) + ':' + addZ(mins) + ':' + addZ(secs);
  };
});
